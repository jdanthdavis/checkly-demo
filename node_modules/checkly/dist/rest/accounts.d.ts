import type { AxiosInstance } from 'axios';
export interface AddonTier {
    tier: string;
    tierDisplayName: string;
}
export interface Account {
    id: string;
    name: string;
    runtimeId: string;
    plan?: string;
    planDisplayName?: string;
    addons?: Record<string, AddonTier>;
    /**
     * Client-visible account feature flags. Absent when the API predates them,
     * in which case standard limits apply.
     */
    features?: string[];
}
declare class Accounts {
    api: AxiosInstance;
    constructor(api: AxiosInstance);
    getAll(): Promise<import("axios").AxiosResponse<Account[], any, {}, any>>;
    get(accountId: string): Promise<import("axios").AxiosResponse<Account, any, {}, any>>;
}
export default Accounts;
