import { type AxiosInstance } from 'axios';
import { GitInformation } from '../services/util.js';
import { RetryStrategy, SharedFile } from '../constructs/index.js';
import { SequenceId } from '../services/abstract-check-runner.js';
import type { CheckResult } from './check-results.js';
type RunTestSessionRequest = {
    name: string;
    checkRunJobs: any[];
    project: {
        logicalId: string;
    };
    sharedFiles?: SharedFile[];
    runLocation: string | {
        type: 'PUBLIC';
        region: string;
    } | {
        type: 'PRIVATE';
        slugName: string;
        id: string;
    };
    repoInfo?: GitInformation | null;
    environment?: string | null;
    shouldRecord: boolean;
    streamLogs?: boolean;
    refreshCache?: boolean;
};
type TriggerTestSessionRequest = {
    name: string;
    runLocation: string | {
        type: 'PUBLIC';
        region: string;
    } | {
        type: 'PRIVATE';
        slugName: string;
        id: string;
    };
    shouldRecord: boolean;
    targetTags: string[][];
    checkId?: string[];
    checkRunSuiteId: string;
    environmentVariables: Array<{
        key: string;
        value: string;
    }>;
    repoInfo: GitInformation | null;
    environment: string | null;
    testRetryStrategy: RetryStrategy | null;
    refreshCache?: boolean;
};
export type TestResultsShortLinks = {
    testResultLink: string;
    testTraceLinks: string[];
    videoLinks: string[];
    screenshotLinks: string[];
};
export type TriggerTestSessionResponse = {
    checks: Array<any>;
    testSessionId: string;
    testResultIds: Record<string, string>;
    sequenceIds: Record<string, SequenceId>;
};
export type RunTestSessionResponse = {
    /** The scheduling operation dispatching this run's check runs in the background. */
    schedulingId: string;
    /** Only present for recorded runs (shouldRecord: true). */
    testSessionId?: string;
    testResultIds?: Record<string, string>;
    sequenceIds: Record<string, SequenceId>;
};
export type TestSessionSchedulingStatus = 'PENDING' | 'RUNNING' | 'SUCCEEDED' | 'FAILED';
export type TestSessionSchedulingOperation = {
    schedulingId: string;
    testSessionId: string | null;
    status: TestSessionSchedulingStatus;
    checksTotal: number;
    error: {
        code: string;
        message: string;
    } | null;
    createdAt: string;
    startedAt: string | null;
    endedAt: string | null;
};
/**
 * The background dispatch of the test session's check runs failed (or the
 * worker died — errorCode ABANDONED): no results will arrive for the
 * undispatched checks, so the run should fail immediately instead of waiting
 * out the per-check result timeouts.
 */
export declare class TestSessionSchedulingFailedError extends Error {
    /** The operation's error code (e.g. SCHEDULING_ERROR, ABANDONED), when known. */
    code?: string;
    constructor(message: string, options?: ErrorOptions & {
        code?: string;
    });
}
export type TestSessionMetadata = {
    environment?: string;
    repoUrl?: string;
    commitId?: string;
    commitOwner?: string;
    commitMessage?: string;
    branchName?: string;
    github?: GitInformation['github'];
};
export type TestSessionStatus = 'RUNNING' | 'FAILED' | 'PASSED' | 'CANCELLED';
export type TestSessionProvider = 'GITHUB' | 'VERCEL' | 'API' | 'TRIGGER' | 'PW_REPORTER';
export type TestSessionResult = {
    testSessionResultId: string;
    testSessionResultLink: string;
    checkId?: string | null;
    checkType: string;
    name?: string;
    runLocation?: string;
    resultType?: string;
    status: TestSessionStatus;
    hasErrors: boolean;
    hasFailures: boolean;
    isDegraded: boolean;
    aborted: boolean;
    errorGroupIds?: string[];
};
export type TestSessionDetail = {
    testSessionId: string;
    testSessionLink: string;
    name: string;
    status: TestSessionStatus;
    startedAt: string;
    stoppedAt: string | null;
    timeElapsed: number;
    metadata?: TestSessionMetadata;
    errorGroupIds?: string[];
    results?: TestSessionResult[];
};
type TestSessionListEntryInvoker = {
    name: string;
    picture?: string | null;
} | null;
export type TestSessionListEntry = {
    id: string;
    accountId: string;
    projectId?: string | null;
    name: string;
    provider: string;
    running?: string[] | null;
    passed?: string[] | null;
    failed?: string[] | null;
    cancelled?: string[] | null;
    status?: TestSessionStatus;
    region: string;
    privateLocationId?: string | null;
    invoker?: TestSessionListEntryInvoker;
    repoUrl?: string | null;
    commitId?: string | null;
    commitOwner?: string | null;
    commitMessage?: string | null;
    branchName?: string | null;
    environment?: string | null;
    startedAt: string;
    stoppedAt?: string | null;
    created_at: string;
    updated_at?: string | null;
};
export type ListTestSessionsParams = {
    from?: number;
    to?: number;
    limit?: number;
    statuses?: TestSessionStatus[];
    branches?: string[];
    users?: string[];
    providers?: TestSessionProvider[];
    noUsers?: boolean;
    nextId?: string;
    textSearch?: string;
    errorGroupId?: string;
};
export type TestSessionsListResponse = {
    length: number;
    entries: TestSessionListEntry[];
    nextId?: string | null;
};
export declare class NoMatchingChecksError extends Error {
    constructor(options?: ErrorOptions);
}
declare class TestSessions {
    api: AxiosInstance;
    constructor(api: AxiosInstance);
    run(payload: RunTestSessionRequest): Promise<import("axios").AxiosResponse<RunTestSessionResponse, any, {}, any>>;
    /**
     * Long-poll the dispatch status of an asynchronously started test-session
     * run. The server blocks for up to `maxWaitSeconds` and returns the
     * operation once it reaches a terminal state, or responds 408
     * ({@link RequestTimeoutError}) while dispatch is still in progress — the
     * retry cadence lives in the caller.
     */
    getSchedulingCompletion(schedulingId: string, options?: {
        maxWaitSeconds?: number;
        signal?: AbortSignal;
    }): Promise<import("axios").AxiosResponse<TestSessionSchedulingOperation, any, {}, any>>;
    pollSchedulingUntilComplete(schedulingId: string, options?: {
        signal?: AbortSignal;
    }): Promise<TestSessionSchedulingOperation>;
    /**
     * @throws {NoMatchingChecksError} If no checks matched the request.
     */
    trigger(payload: TriggerTestSessionRequest): Promise<import("axios").AxiosResponse<TriggerTestSessionResponse, any, {}, any>>;
    get(id: string): Promise<import("axios").AxiosResponse<TestSessionDetail, any, {}, any>>;
    getResult(id: string, resultId: string): Promise<import("axios").AxiosResponse<CheckResult, any, {}, any>>;
    getCompletion(id: string, maxWaitSeconds?: number): Promise<import("axios").AxiosResponse<TestSessionDetail, any, {}, any>>;
    pollUntilComplete(id: string): Promise<TestSessionDetail>;
    list(params?: ListTestSessionsParams): Promise<import("axios").AxiosResponse<TestSessionsListResponse, any, {}, any>>;
    getShortLink(id: string): Promise<import("axios").AxiosResponse<{
        link: string;
    }, any, {}, any>>;
    getResultShortLinks(testSessionId: string, testResultId: string): Promise<import("axios").AxiosResponse<TestResultsShortLinks, any, {}, any>>;
}
export default TestSessions;
